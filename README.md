# Action Repository

This repository triggers GitHub webhooks for testing webhook integration.

## Purpose
- Generates Push events when code is pushed
- Generates Pull Request events when PRs are created
- Sends webhook events to webhook-repo endpoint

## Webhook Events
- Push to any branch
- Pull request creation
- Pull request merge

## Test Update
This line was added to test the webhook integration.
